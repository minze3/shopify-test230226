.dex-promotion-main {

    .dex-promotion-wrapper {
        padding: calc(2rem / var(--font-body-scale));
        text-align: left;
        max-width: 40rem;
        img {
            width:60%;
            display: block;
        }
    }
}
