{{ 'yang-base.scss' | asset_url | stylesheet_tag }}
{{ 'yang-promotion.scss.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .dex-promotion-header {
    background-image: url({{'yang-promotion01.png'|asset_url}});
  }
{%- endstyle -%}

<div class="dex-main dex-promotion-main">
  <section class="dex-promotion-header">
    <div class="dex-promotion-wrapper page-width">
      <img src="{{ 'yang-promotion02.png' | asset_url }}" width="60%">
    </div>
  </section>
</div>
